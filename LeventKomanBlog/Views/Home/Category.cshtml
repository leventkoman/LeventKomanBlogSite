@model IEnumerable<LeventKomanBlog.Models.Category>

    <p class="lead">Kategoriler</p>
    <div class="row">
        <div class="col-lg-12">


            @*<a href="@Url.Action("Index","Home")" class="list-group-item"> <span class="glyphicon glyphicon-arrow-right"></span> Tümü</a>*@

            @foreach (var item in Model)
            {
                <ul class="list-unstyled">
                    <li>
                        <a href="/Home/ByCategory/@item.Id" title="@item.CategoryName" class="list-group-item">
                            <span class="glyphicon glyphicon-arrow-right"></span> @item.CategoryName (@item.CategoryName.Count()
                        </a>
                    </li>
                </ul>
            }



        </div>
    </div>



@*<h4>Blog Categories</h4>
    <div class="row">
        <div class="col-lg-6">
            @foreach (var item in Model)
            {
                <ul class="list-unstyled">
                    <li>
                        <a href="/Home/Category/@item.Id">@item.CategoryName</a>
                    </li>
                </ul>
            }
        </div>
    </div>*@

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryName)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }

    </table>*@
